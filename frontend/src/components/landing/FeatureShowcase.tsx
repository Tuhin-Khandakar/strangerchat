import React from 'react';

export function FeatureShowcase() {
  // This component is currently handled inline in page.tsx if needed
  // But for the sake of modularity, I'll provide a placeholder or skip if not strictly required
  // The app/page.tsx actually defines a FeatureCard internally.
  return null;
}
